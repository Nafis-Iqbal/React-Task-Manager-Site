export enum role{
    user = "user",
    manager = "manager",
    admin = "admin"
}

export enum statusEnum{
    active = "active",
    completed = "completed",
    paused = "paused",
    cancelled = "cancelled"
}

export enum priority{
    urgent = "urgent",
    normal = "normal"
}

export{}